import 'package:flutter/material.dart';

void main() {
  runApp(const Lapp());
}
class Lapp extends StatelessWidget {
     const Lapp({super.key});


 @override
 Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Center(
            child: GridView.count(
              crossAxisCount: 2,
              children: <Widget>[
                Container(color: Colors.red, child: Text('First')),
                Container(color: Colors.blue, child: Text('Second')),
                Container(color: Colors.teal, child: Text('Third')),
                Container(color: Colors.black, child: Text('Fourth')),

              ],

            ),


        ),
      ),
    );


   }

}

